import React from "react";

import SigninComponent from "."

const Signin = () => {

    return (
        <main className="main pages">
            <SigninComponent />
        </main>
    );
}

export default Signin 